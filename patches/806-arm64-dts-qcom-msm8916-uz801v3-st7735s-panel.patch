--- a/arch/arm64/boot/dts/qcom/msm8916-yiming-uz801v3.dts
+++ b/arch/arm64/boot/dts/qcom/msm8916-yiming-uz801v3.dts
@@ -3,33 +3,248 @@
 /dts-v1/;
 
 #include "msm8916-ufi.dtsi"
+#include <dt-bindings/pinctrl/qcom,pmic-mpp.h>
 
 / {
 	model = "uz801 v3.0 4G Modem Stick";
 	compatible = "yiming,uz801-v3", "qcom,msm8916";
 };
 
-&button_restart {
-	gpios = <&tlmm 23 GPIO_ACTIVE_LOW>;
-};
-
 &led_b {
 	gpios = <&tlmm 6 GPIO_ACTIVE_HIGH>;
-};
-
-&led_g {
-	gpios = <&tlmm 8 GPIO_ACTIVE_HIGH>;
+	function = LED_FUNCTION_WLAN;
 };
 
 &led_r {
 	gpios = <&tlmm 7 GPIO_ACTIVE_HIGH>;
 };
 
-&button_default {
-	pins = "gpio23";
-	bias-pull-up;
+/*
+ * SIM slot control. This board (MF601 family) gates the SIM through four
+ * lines; the uz801 profile does not describe them at all, so they float and
+ * the modem reports sim-missing even with a card inserted.
+ * Values taken from the vendor msm8916-ufi-mf601.dts.
+ * Exposed as LEDs so the combination can be tried at runtime via
+ * the sim: LED entries under /sys/class/leds without rebuilding.
+ */
+&{/leds} {
+	sim_sel: sim-sel {
+		gpios = <&tlmm 49 GPIO_ACTIVE_HIGH>;
+		label = "sim:sel";
+		default-state = "on";
+	};
+
+	sim_en: sim-en {
+		gpios = <&tlmm 24 GPIO_ACTIVE_HIGH>;
+		label = "sim:en";
+		default-state = "off";
+	};
+
+	sim_sel2: sim-sel2 {
+		gpios = <&tlmm 50 GPIO_ACTIVE_HIGH>;
+		label = "sim:sel2";
+		default-state = "off";
+	};
+
+	sim_en2: sim-en2 {
+		gpios = <&tlmm 51 GPIO_ACTIVE_HIGH>;
+		label = "sim:en2";
+		default-state = "off";
+	};
 };
 
 &gpio_leds_default {
-	pins = "gpio6", "gpio7", "gpio8";
+	pins = "gpio6", "gpio7", "gpio24", "gpio49", "gpio50", "gpio51";
+};
+
+/*
+ * ST7735S 128x128 SPI panel (MF601-family board).
+ *
+ * Wiring taken from the stock Android device tree of this unit
+ * (qcom,board-id = <0x8 0x100>):
+ *   aliases { spi0 = "/soc/spi@78b7000"; }  -> blsp_spi3
+ *   qcom,platform-spi-dc-gpio = <&tlmm 23>
+ *   qcom,platform-reset-gpio  = <&tlmm 25>
+ *   vdd-supply   -> pm8916 l17 (2.85V)
+ *   vddio-supply -> pm8916 l6  (1.8V)
+ *   backlight    -> PM8916 MPP4, driven as a current sink by the PMIC's
+ *                   PWM/LPG block (stock: qcom,leds-qpnp "lcdwled-backlight",
+ *                   source-sel=1, 10mA). Plain GPIO output does NOT light it.
+ *
+ * The upstream uz801 pin mapping conflicts with the panel and is dropped:
+ *   button_restart was on GPIO23 (= panel DC)
+ *   led_g          was on GPIO8  (= blsp_spi3 MOSI)
+ */
+/ {
+	/delete-node/ gpio-keys;
+
+	/*
+	 * Cell fitted by the user: 815060AR, Li-ion, 3.8V nominal, 3000mAh,
+	 * 11.4Wh, label says 4.35V max charge.
+	 *
+	 * We deliberately charge to 4.20V, not the 4.35V on the label: an
+	 * unbranded cell may well be an ordinary 4.2V one, and charging that
+	 * to 4.35V means permanent overcharge. 4.20V is safe either way and
+	 * only costs some capacity on a genuine high-voltage cell.
+	 */
+	bat: battery {
+		compatible = "simple-battery";
+		voltage-min-design-microvolt = <3400000>;
+		voltage-max-design-microvolt = <4200000>;
+		energy-full-design-microwatt-hours = <11400000>;
+		charge-full-design-microamp-hours = <3000000>;
+	};
+
+	backlight: backlight {
+		compatible = "pwm-backlight";
+		pwms = <&pm8916_pwm 0 100000>;
+		brightness-levels = <0 4 8 16 32 64 128 255>;
+		num-interpolated-steps = <32>;
+		default-brightness-level = <255>;
+		status = "okay";
+	};
+};
+
+/delete-node/ &led_g;
+
+&pm8916_pwm {
+	status = "okay";
+	pinctrl-names = "default";
+	pinctrl-0 = <&pwm_out>;
+};
+
+&pm8916_mpps {
+	/*
+	 * The stock DTB programs MODE_CTL = 0x60 on this MPP: mode bits 110 =
+	 * current sink. It is an LED-style sink feeding the panel backlight,
+	 * not a logic output - configuring it as "digital" only produces a
+	 * glitch when the level changes and never lights the panel.
+	 * Keep the sink, but let the PMIC's PWM drive it through DTEST so
+	 * brightness stays controllable.
+	 */
+	pwm_out: mpp4-state {
+		pins = "mpp4";
+		function = "sink";
+		output-low;
+		qcom,dtest = <1>;
+		power-source = <PM8916_MPP_VPH>;
+	};
+};
+
+&pm8916_bms {
+	status = "okay";
+	monitored-battery = <&bat>;
+};
+
+&pm8916_rpm_regulators {
+	pm8916_l17: l17 {
+		regulator-min-microvolt = <2850000>;
+		regulator-max-microvolt = <2850000>;
+		regulator-always-on;
+	};
+};
+
+&pm8916_l6 {
+	regulator-always-on;
+};
+
+&blsp_spi3 {
+	status = "okay";
+
+	/*
+	 * blsp_spi3_default muxes GPIO10 to plain "gpio" function and parks it
+	 * high, so the controller's native chip select never reaches the pin
+	 * and the panel is never selected. Drive it as a GPIO CS instead
+	 * (spi-qup sets use_gpio_descriptors, and apq8016-schneider-hmibsc
+	 * does the same on this SoC family).
+	 */
+	cs-gpios = <&tlmm 10 GPIO_ACTIVE_LOW>;
+
+	panel@0 {
+		compatible = "galaxycore,gc9107";
+		reg = <0>;
+		spi-max-frequency = <16000000>;
+
+		reset-gpios = <&tlmm 25 GPIO_ACTIVE_LOW>;
+		dc-gpios = <&tlmm 23 GPIO_ACTIVE_HIGH>;
+
+		vdd-supply = <&pm8916_l17>;
+		vddio-supply = <&pm8916_l6>;
+
+		backlight = <&backlight>;
+
+		width-mm = <23>;
+		height-mm = <23>;
+
+		buswidth = <8>;
+		width = <128>;
+		height = <128>;
+		rotate = <0>;
+		fps = <30>;
+		debug = <1>;
+	};
+};
+
+/*
+ * Charging and state of charge.
+ *
+ * Every number is what the stock Android device tree programmed for this
+ * board, read back from the EDL dump (dtb_49.dts): vddmax 4200 mV, vddsafe
+ * 4350 mV, ibatsafe 1000 mA, cutoff 3.30 V, and the discharge curve below is
+ * the vendor's own pc-temp-ocv-lut at 25 C.
+ */
+&bat {
+	/* Vendor cutoff, and the point the curve below ends at. */
+	voltage-min-design-microvolt = <3300000>;
+
+	/*
+	 * pm8916-bms-vm is a voltage-mode gauge: without a curve it cannot turn
+	 * volts into a percentage and simply does not publish "capacity" at
+	 * all, which is why the dashboard had nothing to show.
+	 */
+	ocv-capacity-celsius = <25>;
+	ocv-capacity-table-0 = <4189000 100>, <4073000 95>, <4044000 90>, <4039000 85>, <4032000 80>, <4025000 75>, <4008000 70>, <3974000 65>, <3953000 60>, <3912000 55>, <3884000 50>, <3858000 45>, <3849000 40>, <3839000 35>, <3816000 30>, <3774000 25>, <3690000 20>, <3647000 16>, <3611000 13>, <3571000 11>, <3556000 10>, <3542000 9>, <3529000 8>, <3507000 7>, <3480000 6>, <3439000 5>, <3402000 4>, <3398000 3>, <3367000 2>, <3336000 1>, <3300000 0>;
+};
+
+&pm8916_charger {
+	monitored-battery = <&bat>;
+
+	/*
+	 * Safety trips, not the setpoint: the driver takes the charge voltage
+	 * from the battery node (4.20 V) and uses safe-current as the actual
+	 * fast-charge current.
+	 */
+	qcom,fast-charge-safe-voltage = <4350000>;
+	qcom,fast-charge-safe-current = <1000000>;
+
+	status = "okay";
+};
+
+/*
+ * The charger registers an extcon of its own. pm8916_usbin claims the same
+ * register block (0x1300) and the usb_vbus interrupt, so running both makes
+ * them fight and the USB gadget loses its VBUS source - that is what broke
+ * USB networking the first time the charger was enabled.
+ */
+&pm8916_usbin {
+	status = "disabled";
+};
+
+&usb {
+	extcon = <&pm8916_charger>;
+};
+
+&usb_hs_phy {
+	extcon = <&pm8916_charger>;
+};
+
+/*
+ * Tell the gauge where its power comes from. pm8916-bms-vm derives STATUS
+ * from power_supply_am_i_supplied(), which walks this link. It belongs on the
+ * gauge itself, not on the battery node: put on the battery it is silently
+ * ignored, and the gauge keeps answering Discharging while the charger is
+ * plainly pushing current into the cell.
+ */
+&pm8916_bms {
+	power-supplies = <&pm8916_charger>;
 };
